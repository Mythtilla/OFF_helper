# Cloudflare Workers — static site deployment for the OFF marketing site.
# NOTE: intentionally a separate worker from the main OFF app ("off").
name = "off-helper"
compatibility_date = "2024-11-01"

[assets]
directory = "./public"
not_found_handling = "single-page-application"